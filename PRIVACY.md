# VtfPlayer 隐私说明

这个程序不联网，也不收集任何数据。

## 不联网

随包的 FFmpeg 在编译时就关掉了网络支持（`--disable-network`），程序本身
也没有任何联网代码。它不会向任何服务器发送数据，不做统计，不上报崩溃，
没有账号，也没有广告。

## 在你的电脑上存什么

全部在 `%APPDATA%\VtfPlayer\native\` 这一个文件夹底下：

```
   library.db    你加进来的文件夹、视频信息、缩略图、
                 观看进度、播放列表、时刻收藏、设置
   window.txt    窗口位置和大小
   log.txt       最近一次运行的日志（含崩溃时的调用栈）
```

这些文件只留在本机，不会离开你的电脑。

## 读什么

只读取你自己加进媒体库的文件夹、以及你打开或拖进来的文件。
不扫描这些位置以外的任何地方。

## 怎么删

在「媒体库」页点「清除所有数据」，或者直接删掉上面那个文件夹。
卸载不会自动删它 —— 重装之后你的媒体库还在。

适用于 VtfPlayer for Windows 1.0.0 及以后的版本。最后更新：2026-09-17。
